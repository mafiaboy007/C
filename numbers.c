#include <stdio.h>
#include <math.h>

int main() {
	/* We r using simple math functions to square, ceil, floor and power numbers */
	printf("%.0f \n", sqrt(81));
	printf("%.0f \n", ceil(1.23));
	printf("%.0f \n", floor(1.23));
	printf("%.0f \n", pow(5, 5));
	return 0;
}
